name: annix
description: Project Anni client built with flutter.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  collection: ^1.16.0

  path: ^1.8.2
  path_provider: ^2.0.14
  f_logs:
    git:
      url: https://github.com/ProjectAnni/Flogs
  flutter_hooks: ^0.18.6
  hooks_riverpod: ^2.3.2
  riverpod_annotation: ^2.0.2
  connectivity_plus: ^3.0.2

  dio: ^5.1.1
  dio_cookie_manager: ^2.1.4
  dio_smart_retry: ^5.0.0
  http: ^0.13.5
  cookie_jar: ^3.0.1

  netease_music_api:
    git:
      url: https://github.com/ProjectAnni/flutter-netease-music-api.git
  xml: ^6.2.2

  audioplayers: ^3.0.1
  audio_service: ^0.18.9
  audio_service_platform_interface: ^0.1.0
  audio_session: ^0.1.13
  anni_mpris_service: ^0.1.0

  slang: ^3.15.0
  slang_flutter: ^3.15.0

  audio_video_progress_bar: ^1.0.1
  extended_image:
    git:
      url: https://github.com/chaudharydeepanshu/extended_image
      ref: 6c0fcecaad16a921bf9d822ac169fd1bad45d1bb
  flutter_lyric: ^2.0.4+6
  settings_ui: ^2.0.2
  cupertino_icons: ^1.0.5
  flutter_staggered_grid_view: ^0.6.2
  flutter_linkify: ^5.0.2

  we_slide: ^2.4.0
  animations: ^2.0.5
  crypto: ^3.0.2

  json_serializable: ^6.6.1
  json_annotation: ^4.8.0

  drift: ^2.6.0
  sqlite3_flutter_libs: 0.5.10

  uuid: ^3.0.7

  responsive_framework: ^0.2.0
  bitsdojo_window: ^0.1.5
  file_picker: ^5.2.7
  url_launcher: ^6.1.10

  # reverse proxy
  shelf: ^1.4.0
  shelf_router: ^1.1.3

  pool: ^1.5.1
  share_plus: ^6.3.0

  # file 6.12 -> 6.14 is breaking, specify version manually
  file: ^6.1.4
  ffi: ^2.0.1
  flutter_rust_bridge: ^1.72.2
  freezed_annotation: ^2.2.0
  meta: ^1.8.0

dev_dependencies:
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter
  drift_dev: ^2.6.0
  build_runner: ^2.4.1
  custom_lint: ^0.3.2
  slang_build_runner: ^3.15.0
  ffigen: ^7.2.10
  freezed: ^2.3.2
  riverpod_generator: ^2.1.4
  riverpod_lint: ^1.1.6

flutter:
  uses-material-design: true

  assets:
    - assets/icon.png
    - assets/silent.wav

dependency_overrides:
  cli_util: 0.4.0
