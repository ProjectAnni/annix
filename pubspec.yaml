name: annix
description: Project Anni client built with flutter.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  get: ^4.6.1
  shared_preferences: ^2.0.9
  archive: ^3.1.6
  connectivity_plus: ^2.3.0
  dio: ^4.0.4
  dio_cookie_manager: ^2.0.0
  dio_http2_adapter: ^2.0.0
  cookie_jar: ^3.0.1

  just_audio: ^0.9.21
  just_audio_background: ^0.0.1-beta.5
  # just_audio_libwinmedia:
  #   git:
  #     url: https://github.com/ProjectAnni/just_audio_libwinmedia.git
  #     ref: fork
  audio_video_progress_bar: ^0.10.0
  extended_image:
    git:
      url: https://github.com/ProjectAnni/extended_image.git

  toml: ^0.12.0
  json_serializable: ^6.1.1
  json_annotation: ^4.4.0
  sqflite: ^2.0.1
  # sqflite_common_ffi: ^2.1.0
  uuid: ^3.0.5

  # bitsdojo_window: ^0.1.1+1
  file_picker: ^4.2.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.5
  flutter_ume: ^0.3.0+1
  flutter_ume_kit_ui: ^0.3.0+1
  flutter_ume_kit_device: ^0.3.0
  flutter_ume_kit_perf: ^0.3.0
  flutter_ume_kit_show_code: ^0.3.0
  flutter_ume_kit_console: ^0.3.0
  flutter_ume_kit_dio: ^0.3.0

flutter:
  uses-material-design: true
